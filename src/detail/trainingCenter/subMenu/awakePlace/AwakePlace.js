import * as Styled from '../../../../funcSet/styledSet';
import Template from '../Template';

const AwakePlace = () => {
  const num = 0;
  return (
    <Styled.StyledDiv padding={'0'} className="awake_place_wrapper">
      <Template id={num} />
    </Styled.StyledDiv>
  )
}

export default AwakePlace;